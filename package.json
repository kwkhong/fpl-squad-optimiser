{
  "name": "fpl-optimal-xi",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node --check app.js && node --check engine.mjs && node --check scripts/update-fpl-data.mjs && node --check scripts/backtest-model.mjs && node --check scripts/backtest-historical.mjs && npm test"
  }
}
